# HealthCompanion — AI-powered Educational Triage Assistant

**HealthCompanion** is an educational, AI-powered health triage companion designed to help users explore symptoms, understand general medical concepts, and learn about potential healthcare pathways. By leveraging the fast Groq API (running Llama 3) in **JSON Mode** for structured reasoning, the app helps users assess conditions and understand warnings in a structured format.

> [!WARNING]
> **CRITICAL MEDICAL DISCLAIMER: EDUCATIONAL USE ONLY**
>
> HealthCompanion is **NOT** a diagnostic tool and does **NOT** provide medical diagnoses, treatment plans, or drug prescriptions. All information, content, and outputs generated by this application are for general educational and informational purposes only. It should never be treated as a substitute for professional medical advice, consultation, diagnosis, or treatment.
>
> Always consult a doctor or a qualified healthcare provider regarding any medical conditions, symptoms, or health questions you may have. Never disregard professional medical advice or delay seeking it because of something you read or heard on this platform. **If you are experiencing a medical emergency, please contact your local emergency services (e.g., 911 or equivalent) immediately.**

---

## 🌟 Key Features

1. **Symptom Quick-Tap Chips**: Pill-shaped symptom chips (Fever, Cough, Nausea, Headache, etc.) to quickly add symptoms to the text input for immediate combination and search.
2. **Clarifying Follow-Up Flow**: Backend prompt instructs the AI to ask 1-2 clarifying questions when symptom descriptions are vague, holding the triage badge until sufficient details are provided.
3. **Context-Aware Memory**: Submits the last 6 messages in the payload history to keep conversational state active and avoid repeating requests.
4. **Vitals Panel Integration**: Optional form fields allowing users to report Temperature (°F), Blood Pressure (Systolic/Diastolic), and Pulse rate (BPM) which are analyzed directly by the AI model.
5. **Clean PDF Export**: Custom jsPDF exporter that generates structured documents with role tags, bulleted potential causes/red flags, warning badges, and a safety disclaimer. Automatically saved with timestamp filenames.
6. **Class-Based Dark Mode**: Easy sun/moon toggle in the header that persists the user's preference in `localStorage`.
7. **JSON Mode Guardrails**: Enforces structured JSON responses from Groq API to eliminate parsing glitches, with safety fallback handlers.
8. **Rate Limiting**: Built-in `slowapi` guard rails to restrict clients to **15 requests per minute** to protect the backend from abuse.

---

## 🛠️ Technical Stack

* **Backend**: Python, FastAPI, Groq API (JSON Mode), python-dotenv, Uvicorn, Slowapi
* **Frontend**: React (Create React App), Tailwind CSS, jsPDF, Web Speech API (for optional text-to-speech output and microphone input)
* **Storage**: Browser LocalStorage for persistent local chat logs and theme states

---

## 🚀 Getting Started

Refer to the setup guides in the root workspace folder to configure and run the applications locally:

### 1. Backend Configuration
Create a `.env` file inside `health-backend/` with the following:
```env
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant
ALLOWED_ORIGIN=*
```
To run the backend:
```bash
cd health-backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### 2. Frontend Configuration
Ensure dependencies (like `jspdf`) are installed:
```bash
cd health-frontend
npm install
npm start
```

---

## 🌐 Production Deployment

Refer to `DEPLOY.md` for a comprehensive step-by-step layout on how to deploy this application to **Render** (FastAPI backend) and **Vercel** (React frontend).
